import React from "react";

import { Box } from "@mui/material";

// This is a template that can be used for pages. Contains sections to place a header, footer, and main body content.
// We need to wait on the header and footer to be completed before this can be completely done

function PageTemplate() {
  return (
    <Box sx={{ flexGrow: 1 }}>
      {/* Header can go here */}

      {/* Main body content can go here */}

      {/* Footer can go here */}
    </Box>
  );
}

export default PageTemplate;
